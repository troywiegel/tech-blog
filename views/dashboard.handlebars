<div class="d-flex justify-content-center">
    <h1>Dashboard | All the blogs you have posted!</h1>
</div>
<br>
<div class="d-flex justify-content-center">
    <a id="newPost" class="btn btn-primary text-white">Add a new blogpost!</a>
</div>
<br>
<div class="d-flex container-fluid justify-content-center">
    <div class="column col-9">
        {{#each myBlogPosts}}
        <div class="card col my-3 bg-light">
            <div class="card-body bg-light text-dark">
                <h3 class="card-title">{{title}}</h3>
                <h4 class="card-subtitle">{{body}}</h4>
                <br><br>
                <h5 class="card-subtitle">Created by: {{user.username}}</h5>
                <h5 class="card-subtitle">Posted on: {{format_date createdAt}}</h5>
                <div class="float-right">
                    <a name={{id}} class="btn btn-primary text-white deletePost">Delete</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>