<div class="d-flex container-fluid justify-content-center">
    <div class="column col-9">
        {{#each onePost}}
        <div class="card col bg-light">
            <div class="card-body bg-light text-dark">
                <h3 class="card-title">{{title}}</h3>
                <h4 class="card-subtitle">{{body}}</h4>
                <br><br>
                <h5 class="card-subtitle">Created by: {{user.username}}</h5>
                <h5 class="card-text">Posted on: {{format_date createdAt}}</h5>
                <a name={{onePost.id}} id="commentPost" class="btn btn-primary text-white float-right">Add Comment</a>
            </div>
        </div>
        {{/each}}
    </div>
</div>